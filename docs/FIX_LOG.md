# Fund24 Fix Log

Chronological record of audit-driven fixes. Cross-ref: `docs/analysis/audit_findings.json`.

| ID | Severity | Status | PR | Commit | Notes |
|---|---|---|---|---|---|
| F-002 | HIGH | DONE | https://github.com/MasterPlayspots/O.A.F24-v2/pull/11 | `8d3a996` | hono 4.12.8 → 4.12.14 (defensive bump; CVEs were not in npm audit DB for 4.12.8, but GHSA entries recommend 4.12.12+). Smoke test + build green. |
| F-001 | HIGH | DONE | https://github.com/MasterPlayspots/O.A.F24-v2/pull/12 | _tbd_ | Sentry DSGVO: PII-scrubber `lib/sentry/scrubber.ts` + `sendDefaultPii: false` + `tracesSampleRate 1.0 → 0.1` in all 3 frontend inits + worker Toucan. 5 scrubber unit tests green. |
| GAP-001 | HIGH | DONE | https://github.com/MasterPlayspots/O.A.F24-v2/pull/13 | _tbd_ | Admin BAFA Cert-Queue: 3 handlers in `worker/src/routes/admin.ts` (GET pending, POST approve, POST reject) + migration 027 adds `bafa_cert_status`/`_uploaded_at`/`_berater_nr` columns on `users`. 6 vitest cases green. Wrapper `listPendingCerts` unwraps `.certs`. Also unblocks F-009 by removing stale `tsconfig.json` extends. Remote migration applied 2026-04-15. |
| F-006 | MED | DONE | _tbd_ | _tbd_ | Removed all 12 `console.*` from prod frontend (`app/`, `lib/`). Error paths now go through `Sentry.captureException` with `tags: {area, op}`. Client-side `requireEnv` fallback removed (API errors already cascade). |
| F-010 | MED | DONE | _tbd_ | _tbd_ | `worker/src/routes/check.ts:72` silent catch replaced with Toucan capture (`tags: route=check, op=scrape-company`). Comment documents that the fallback is intentional (best-effort enrichment). |
| F-013 | MED | DONE | _tbd_ | _tbd_ | `app/error.tsx` useEffect now calls `Sentry.captureException(error, { tags: { boundary: 'app' }, contexts: { digest } })`. |
| F-015 | LOW | DONE | _tbd_ | _tbd_ | SupportWidget: real phone (+49 1512 9617192) + email (support@fund24.io) + hours were already in place — dropped the leftover TODO comment. |
| F-018 | LOW | DONE | _tbd_ | _tbd_ | `worker/src/middleware/cors.ts`: DEV origins, Vercel preview pattern (`fund24-*.vercel.app`), and v0.dev domains now gated behind `env !== "production"`. Also dropped legacy `v0-bafa-creator-ai.vercel.app` fallback. |
| F-012 / GAP-005 | MED | DONE | _tbd_ | _tbd_ | Deleted two unbound empty R2 buckets — `fund24-dokumente` (0 objects, created 2026-04-07) and `fund24-company-files` (0 objects, created 2026-01-23). Decision: **Option A (delete)**. Zero code references, zero data loss. Active upload paths are unchanged: `bafa-reports` (bound as `REPORTS` in `worker/wrangler.toml`) and `foerdermittel-check-docs` (bound in the check worker). A future BAFA-cert feature (GAP-002) will create its own `fund24-bafa-certs` bucket per that fix-prompt. |
| F-011 / GAP-007 | MED | DONE | _tbd_ | _tbd_ | New `scripts/gen-api-docs.ts` walks `worker/src/routes/**/*.ts`, identifies Hono router variables via `new Hono()` declarations, and emits `docs/API.md` — **143 endpoints grouped into 23 groups** with source links. Hooked into `npm run docs:api`; `.github/workflows/docs-check.yml` blocks PRs when the file is out of sync. |
| F-016 | LOW | DONE | _tbd_ | _tbd_ | `docs/MIGRATIONS.md` documents the D1 workflow, DB-binding map, rollback-SQL rules, and emergency recovery. Every pre-existing migration now has a `*-rollback.sql` companion (16 foundational files marked `NOT REVERSIBLE` with R2-backup recovery note, 9 feature migrations get real `DROP TABLE` rollbacks, 3 drop-FK migrations marked `NOT REVERSIBLE` per doc policy). CI `docs-check.yml` blocks PRs that add a new migration without a rollback pair. |
| GAP-003 | MED | DONE | _tbd_ | _tbd_ | `ECOSYSTEM.md` now carries a deprecation banner pointing at `docs/API.md` (auto-generated) and `docs/MIGRATIONS.md`. Lists the stale entries (`/api/bafa`, `/api/forum/*`, webauthn, magic-link) and the ones missing from the doc (`/api/antraege/*`, `/api/me/dashboard`, tracker, OA, news, onboarding cron). `README.md` Documentation section rewritten. |
| F-007 | MED | DONE | _tbd_ | _tbd_ | `as any` / `: any` sweep: 60 → 45 occurrences total, **21 → 6 in non-test code**. Fixes: zod schemas for PayPal + Stripe responses, `ZodIssue` type in auth.ts, typed D1 row generics in backup.ts, `runLlama` helper in check.ts concentrates the CF AiModels cast, typed `FoerdermittelProfileRow` generic on `.first<>()` calls. All 6 remaining non-test `any`s have `eslint-disable-next-line` justifications (CF AI model union + RHF-zod resolver quirk). ESLint rule `no-explicit-any: warn` was already in both configs. |
