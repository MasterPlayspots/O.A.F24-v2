// Programme-Anzahl — single source of truth for the "3.400+" marketing number.
// Kept as a string so callers can render it without a locale formatter.
// TODO: pull from /api/stats once the endpoint is live and cached per-day.
export const PROGRAMME_COUNT = '3.400+'
